import { Module } from '@nestjs/common'; import { BullModule } from '@nestjs/bullmq'; import { NewsService } from './news.service'; import { NewsController } from './news.controller'; import { NewsProcessor, NewsScheduler, NEWS_QUEUE } from './news.scheduler';
@Module({imports:[BullModule.forRoot({connection:{host:process.env.REDIS_HOST||'localhost',port:Number(process.env.REDIS_PORT||6379),password:process.env.REDIS_PASSWORD||undefined}}),BullModule.registerQueue({name:NEWS_QUEUE})],controllers:[NewsController],providers:[NewsService,NewsScheduler,NewsProcessor],exports:[NewsService]}) export class NewsModule {}
