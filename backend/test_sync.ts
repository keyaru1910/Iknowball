import { NestFactory } from '@nestjs/core'; import { AppModule } from './src/app.module'; import { SportsSyncService } from './src/module/sports-sync/sports-sync.service'; async function run() { const app = await NestFactory.createApplicationContext(AppModule); const syncService = app.get(SportsSyncService); try { console.log('Starting syncLiveMatches...'); const res = await syncService.syncLiveMatches(); console.log('Result:', res); } catch(e) { console.error('Error:', e); } await app.close(); } run();
